# Root CLAUDE.local.md is a local symlink to .claude/CLAUDE.local.md so
# Claude Code auto-loads it. The real file (in .claude/) stays tracked;
# the leading slash anchors this to the repo root only.
/CLAUDE.local.md
