# .claude/CLAUDE.md is the tracked global instructions file; ~/.claude/CLAUDE.md
# is a local symlink to it, so Claude Code auto-loads it in every repo. Nothing
# needs ignoring at the root anymore — kept as a placeholder for local-only files.
